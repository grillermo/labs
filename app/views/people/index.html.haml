%section#title
  %h1 
    Los nombres de:

%section#names
  - @people.each do |person|
    =link_to(person_path(person), class: 'fan') do
      = person.name

%footer
  = link_to new_person_path do
    nueva persona
